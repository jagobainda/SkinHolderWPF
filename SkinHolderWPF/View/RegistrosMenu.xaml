<UserControl x:Class="SkinHolderWPF.View.RegistrosMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="511" 
             d:DesignWidth="834"
             Background="#444444">

    <UserControl.Resources>
        <Style x:Key="RoundedPanelStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Margin" Value="30"/>
        </Style>
    </UserControl.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource RoundedPanelStyle}">
            <StackPanel>
                
            </StackPanel>
        </Border>
        
        <Border Grid.Column="1" Style="{StaticResource RoundedPanelStyle}">
            <StackPanel>
                
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
